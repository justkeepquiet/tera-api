<%- contentFor("content") %>
<article class="content">
	<div class="title-block">
		<h1 class="title"><%= __("Shop Accounts") %></h1>
	</div>
	<section class="section">
		<a class="btn btn-success" title="" href="shopAccountAdd"><%= __("Create new account") %></a>
	</section>
	<section class="section">
		<div class="alert alert-secondary">
			<%= __("Accounts are created automatically when funds are deposited to the user's Shop balance.") %>
		</div>
	</section>
	<section class="section">
		<form class="form-inline" action="" method="GET">
			<div class="form-group">
				<label for="account"><%= __("Account ID") %></label>
				<input type="number" class="form-control boxed" name="account" value="<%= account %>">
			</div>
			<button type="submit" class="btn btn-primary"><%= __("Show") %></button>
		</form>
	</section>
	<section class="section">
		<div class="row">
			<div class="col-md-12">
				<div class="card">
					<div class="card-block">
						<section>
							<div class="table-responsive">
							<table class="table table-hover data-table-nosort">
								<thead>
									<tr>
										<th><%= __("Account ID") %></th>
										<th><%= __("Balance") %></th>
										<th><%= __("Active") %></th>
										<th><%= __("Created") %></th>
										<th></th>
									</tr>
								</thead>
								<tbody>
									<% accounts.forEach(account => { %>
									<tr>
										<td><%= account.get("accountDBID") %></td>
										<td><%= account.get("balance") %></td>
										<td><%= account.get("active") ? __("Yes") : __("No") %></td>
										<td><%= moment(account.get("createdAt")).format("YYYY-MM-DD HH:MM:ss") %></td>
										<td class="text-right">
											<a class="btn btn-secondary btn-sm" href="ahopAccountEdit?id=<%= account.get("accountDBID") %>"><i class="fa fa-pencil"></i></a>
											<a class="btn btn-secondary btn-sm" href="#" data-toggle="modal" data-target="#confirm-modal"><i class="fa fa-trash-o "></i></a>
										</td>
									</tr>
									<% }) %>
								</tbody>
							</table>
							</div>
						</section>
					</div>
				</div>
			</div>
		</div>
	</section>
</article>